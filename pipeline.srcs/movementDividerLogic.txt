b0: reset

side note (qol feature):
    right, left, up, down latching regsiters: FROM THE REGFILE
    12,    13,   14, 15: their MSB should be combinationally driving an output, 
   
pointer:
    based on the x and y values (directly tapped from the register bank from r1 and r2)
    we will render another combinational framebuffer, each pixel is basically a function of x and y
    pointer:    
        1 solid pixel at the exact coordinates of x and y
        1 square layer of blank space around it, 1 pixel thick
        1 square layer of solid pixels around the blank space, 1 pixel thick
        so basically it should look like a square crosshair, with 3 layers.
        and this 128*64 crosshair framebuffer should be flattened exactly like my pixel framebuffer.

        but at the end, we want our net framebuffer to contain both the pixel framebuffer and the crosshair 
        framebuffer, right?

        so im going to OR those 2 framebuffers, pixel by pixel, and present that as the net framebuffer.

        


let scan cycle be x clock cycles 
so scan time st = x/(50e6) {seconds} 
let desired speed: v {pixels per second}
    should be adjustable, togglable with 2 buttons 

    rough avg: 4, range: 1 to 10
    b3: reduce, b2: increase
    this count should be tapped to a 4 bit output, which i will assign to fpga's 4 leds later(qol feature)

let unfiltered speed:   vu (basically no of pixels per second when not refined)
                        1*st {s} : 1 {px}
                        1{s} : vu {px/s}  * 1 {s} = vu {px}

                        so vu = 1/st {px/s}

we will achieve desired speed by applying PWM on unfiltered speed.

on avg: 75 instr per loop. so 75cc. at 50Mhz, st = 75/50e6 = 1.5 us
vu = 0.666 million {px/s}
we want v = 4 {px/s} (approx)


so baseline speed reduction factor = vu / v = r = 166666.667= 500,000/3

so to achieve a speed reduction of "r"
let con = no of "ON" clock cycles every 10^6 cycles. 
con = (10^6) / (500,000/3) = 6
so basically 3 cc for every 500k cyckes.
so basically 1cc every 166,666 cycles.
 



